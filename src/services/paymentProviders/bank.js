import { createProviderAdapter } from './base';
export default createProviderAdapter({ id: 'bank', name: 'Bank Transfer', short: 'Bank', region: 'Diaspora', accent: '#1e4233', ussdHint: 'SWIFT / IBAN' });
