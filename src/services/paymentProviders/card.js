import { createProviderAdapter } from './base';
export default createProviderAdapter({ id: 'card', name: 'Debit / Credit Card', short: 'Card', region: 'Diaspora', accent: '#0D4F4A', ussdHint: 'Visa · Mastercard' });
